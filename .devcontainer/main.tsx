import React from 'react';
import ReactDOM from 'react-dom/client';
import MorphologicalAssemblerIDE from './static.ide';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MorphologicalAssemblerIDE />
  </React.StrictMode>
);
